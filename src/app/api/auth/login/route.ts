import{NextResponse}from"next/server";import{z}from"zod";const input=z.object({email:z.email(),password:z.string().min(8)});const output=z.object({success:z.literal(true),data:z.object({accessToken:z.string(),refreshToken:z.string(),expiresAt:z.string()})});export async function POST(request:Request){const parsed=input.safeParse(await request.json());if(!parsed.success)return NextResponse.json({success:false,error:{code:"VALIDATION",message:"Email hoặc mật khẩu không hợp lệ"}},{status:400});try{const upstream=await fetch(`${process.env.API_URL??"http://localhost:3000/api/v1"}/auth/login`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(parsed.data),cache:"no-store"});const body:unknown=await upstream.json();if(!upstream.ok)return NextResponse.json(body,{status:upstream.status});const result=output.safeParse(body);if(!result.success)return NextResponse.json({success:false,error:{code:"INVALID_AUTH_RESPONSE",message:"Invalid authentication response"}},{status:502});const response=NextResponse.json({success:true,data:{authenticated:true}});response.cookies.set("access_token",result.data.data.accessToken,{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:15*60});response.cookies.set("refresh_token",result.data.data.refreshToken,{httpOnly:true,sameSite:"strict",secure:process.env.NODE_ENV==="production",path:"/api/auth",expires:new Date(result.data.data.expiresAt)});return response}catch{return NextResponse.json({success:false,error:{code:"BACKEND_UNAVAILABLE",message:"Authentication service unavailable"}},{status:503})}}
