import{NextResponse}from"next/server";import{z}from"zod";
const input=z.object({publicCode:z.string().min(1),quoteId:z.uuid(),checkIn:z.iso.date(),checkOut:z.iso.date(),guestCount:z.number().int().min(1).max(50),source:z.literal("WEBSITE"),firstName:z.string().trim().min(1),lastName:z.string().trim().min(1),email:z.email().optional().or(z.literal("")),phone:z.string().trim().min(8)});
export async function POST(request:Request){const parsed=input.safeParse(await request.json());if(!parsed.success)return NextResponse.json({success:false,error:{code:"VALIDATION",message:"Invalid booking request",details:parsed.error.flatten()}},{status:400});try{const response=await fetch(`${process.env.API_URL??"http://localhost:3000/api/v1"}/bookings`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...parsed.data,email:parsed.data.email||undefined}),cache:"no-store"});return new NextResponse(await response.text(),{status:response.status,headers:{"content-type":"application/json"}})}catch{return NextResponse.json({success:false,error:{code:"BACKEND_UNAVAILABLE",message:"Booking service is temporarily unavailable"}},{status:503})}}
