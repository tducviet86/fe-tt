"use client";
import Image from"next/image";import{motion,useReducedMotion,useScroll,useTransform}from"motion/react";import{useRef}from"react";
type Photo={media:{url:string;altVi:string|null;altEn:string|null}};
export function DetailGallery({photos,title,locale}:{photos:Photo[];title:string;locale:"vi"|"en"}){const ref=useRef<HTMLDivElement>(null),reduced=useReducedMotion(),{scrollYProgress}=useScroll({target:ref,offset:["start end","end start"]}),y=useTransform(scrollYProgress,[0,1],[-35,35]);if(!photos.length)return <div className="grid min-h-[520px] place-items-center rounded-[28px] bg-[#dfe7df]"><p className="font-display text-3xl">{locale==="vi"?"Ảnh đang được cập nhật":"Photography is coming soon"}</p></div>;return <div ref={ref} className="grid gap-3 md:grid-cols-[1.55fr_.75fr] md:grid-rows-2"><motion.div style={reduced?undefined:{y}} className="relative aspect-[4/3] overflow-hidden rounded-[26px] md:row-span-2 md:aspect-auto"><Image src={photos[0].media.url} alt={photos[0].media[locale==="vi"?"altVi":"altEn"]??title} fill priority sizes="(min-width:768px) 68vw,100vw" className="object-cover scale-[1.08]"/></motion.div>{[photos[1]??photos[0],photos[2]??photos[0]].map((photo,index)=><motion.div key={index} initial={{opacity:0,x:30}} animate={{opacity:1,x:0}} transition={{delay:.25+index*.12,duration:.7}} className="relative hidden min-h-56 overflow-hidden rounded-[22px] md:block"><Image src={photo.media.url} alt={photo.media[locale==="vi"?"altVi":"altEn"]??title} fill sizes="30vw" className="object-cover transition duration-700 hover:scale-105"/></motion.div>)}</div>}
